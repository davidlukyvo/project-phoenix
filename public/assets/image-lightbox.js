(()=>{const root=document.querySelector('article');if(root){const w=document.createTreeWalker(root,NodeFilter.SHOW_TEXT);const nodes=[];while(w.nextNode())nodes.push(w.currentNode);nodes.forEach(n=>{if(n.nodeValue&&n.nodeValue.includes('Tân Hiệp Phát'))n.nodeValue=n.nodeValue.replaceAll('Tân Hiệp Phát','THP')})}const images=[...document.querySelectorAll('article figure img')];if(!images.length)return;const overlay=document.createElement('div');overlay.className='image-lightbox';overlay.setAttribute('role','dialog');overlay.setAttribute('aria-modal','true');overlay.setAttribute('aria-label','Xem hình phóng to');overlay.innerHTML='<div class="image-lightbox__frame"><img class="image-lightbox__img" alt=""><button class="image-lightbox__close" type="button" aria-label="Đóng">×</button><div class="image-lightbox__hint">Click ra ngoài hoặc nhấn Esc để đóng</div></div>';document.body.appendChild(overlay);const full=overlay.querySelector('.image-lightbox__img');const close=overlay.querySelector('.image-lightbox__close');let last=null;const open=img=>{last=img;full.src=img.currentSrc||img.src;full.alt=img.alt||'';overlay.classList.add('open');document.body.classList.add('lb-open');close.focus({preventScroll:true})};const shut=()=>{overlay.classList.remove('open');document.body.classList.remove('lb-open');full.removeAttribute('src');if(last)last.focus({preventScroll:true})};images.forEach(img=>{img.tabIndex=0;img.setAttribute('role','button');img.setAttribute('aria-label',(img.alt?img.alt+'. ':'')+'Nhấn để phóng to hình');img.addEventListener('click',()=>open(img));img.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open(img)}});img.addEventListener('load',()=>{const ratio=img.naturalWidth/img.naturalHeight;if(ratio>=1.45)img.closest('figure')?.classList.add('diagram')},{once:true});if(img.complete&&img.naturalWidth){const ratio=img.naturalWidth/img.naturalHeight;if(ratio>=1.45)img.closest('figure')?.classList.add('diagram')}});close.addEventListener('click',shut);overlay.addEventListener('click',e=>{if(e.target===overlay||e.target.classList.contains('image-lightbox__frame'))shut()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&overlay.classList.contains('open'))shut()})})();